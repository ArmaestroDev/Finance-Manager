// The Edit-transaction modal is a single shared, responsive Sheet-based
// implementation (bottom sheet on native, centered dialog on web). The mobile
// variant re-exports it so the mobile path uses the modern Sheet + Field
// design with no divergent legacy copy.
export { EditTransactionModal } from "../EditTransactionModal";
