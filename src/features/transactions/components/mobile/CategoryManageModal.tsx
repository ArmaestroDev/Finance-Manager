// The category-manage modal is a single shared, responsive Sheet-based
// implementation: category list with color swatches plus the inline
// monthly-estimate/budget editor (CategoryBudgetEditor). The mobile variant
// re-exports it so the mobile path matches desktop capability with no
// divergent legacy copy.
export { CategoryManageModal } from "../CategoryManageModal";
