// The transaction-detail modal is a single shared, responsive Sheet-based
// implementation: amount hero, all detail fields, category assignment with
// create-new via CategoryPickerModal, and Edit/Delete for manual rows. The
// mobile variant re-exports it so the mobile path uses the modern Sheet
// design with no divergent legacy copy.
export { TransactionDetailModal } from "../TransactionDetailModal";
